# About

## Publish flow

```sh
git add .
git commit -m '<message>'

# Update version
# For example update the patch version
npm version patch

# Build project & publish it to npm registry
npm run pub
```
